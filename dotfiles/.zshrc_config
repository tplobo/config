function source_configs () {
    for FILE in $@; do
        source "$HOME/"$FILE;
        #echo $FILE" sourced!";
    done
    }

CONFIGS_FULL=(
    #.zsh_p10k       # Must be first (Homebrew package)
    #.zsh_omz
    .zsh_aliases
	#.zsh_syntax     # Must be last (Homebrew package)
)
CONFIGS_SLIM=(
    #.zsh_p10k
    #.zsh_omz
    .zsh_aliases
	#.zsh_syntax
)
