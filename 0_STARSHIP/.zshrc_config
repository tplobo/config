function source_configs () {
    for FILE in $@; do
        source "$HOME/"$FILE;
        #echo $FILE" sourced!";
    done
    }

CONFIGS_FULL=(
    .zsh_p10k       # Homebrew (must be first)
    .zsh_omz
    .zsh_aliases
	.zsh_syntax     # Homebrew (must be last)
)
CONFIGS_SLIM=(
    #.zsh_p10k
    .zsh_omz
    .zsh_aliases
	.zsh_syntax
)
